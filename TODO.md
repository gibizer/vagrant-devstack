* add global gitconfig with name and email to allow cherry-picking
  git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

* install osc-placement-tree
* install xdot

