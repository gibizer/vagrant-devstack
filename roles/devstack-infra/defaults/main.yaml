---
local_tmp_dir: "tmp"
ssh_access_pub_key: "~/.ssh/id_ed25519.pub"
swap_size: "2G"
packages_present:
  - git
  - xdot
  - acl  # to avoid ansible issues with unprivilieged user
  - openvswitch-switch
packages_absent: []
